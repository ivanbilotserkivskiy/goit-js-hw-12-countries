{{#each this}}
 <h1>{{name}}</h1>
            <img src={{flags.png}}>
            <p>Capital:{{capital}}</p>
            <p>Population:{{population}}</p>
    
            <ul>
                {{#each languages}}
                <li>{{this.name}}</li>
                {{/each}}
            </ul>
            
{{/each}}